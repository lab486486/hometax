---
interface Props {
  size?: number;
  class?: string;
}

const { size = 20, class: className = "" } = Astro.props;
---

<img
  src="/images/brand/hometax-mark.webp"
  alt=""
  width={size}
  height={size}
  class={`brand-logo ${className}`.trim()}
  decoding="async"
  loading="eager"
/>
